# Railway Configuration untuk Backend
[build]
builder = "NIXPACKS"
workingDirectory = "ai-recruitment-backend"

[deploy]
startCommand = "npx prisma migrate deploy && npm run start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
