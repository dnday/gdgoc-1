# Railway Configuration untuk Backend
[build]
builder = "NIXPACKS"
buildCommand = "cd ai-recruitment-backend && npm install && npx prisma generate"

[deploy]
startCommand = "cd ai-recruitment-backend && npx prisma migrate deploy && npm run start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
